const first='Jaan';
const last='pakhi';
const greet='potash potash'
const name=first+" "+last+" "+greet;
console.log(name); 
const a =10;
const b =20;
const result='The sum of'+a+ 'and '+b+'is'+(a+b);
//back tick4
const math=`The sum of ${a} and ${b} is ${a+b}`;
console.log(math)

const email='Hi Jogn Sina'